<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage: Android 6–12 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Storage: Android 14+ (Selected Photos Access) -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Notifications: Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".core.AgroPredictApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgroPredict">

        <activity
            android:name=".presentation.user_interface.screen.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.user_interface.screen.RegisterActivity" />
        <activity android:name=".presentation.user_interface.screen.HomeActivity" />
        <activity android:name=".presentation.user_interface.screen.PredictionActivity" />
        <activity android:name=".presentation.user_interface.screen.PredictionResultActivity" />
        <activity android:name=".presentation.user_interface.screen.HistoryActivity" />
        <activity android:name=".presentation.user_interface.screen.FieldDetailActivity" />
        <activity android:name=".presentation.user_interface.screen.EditFieldActivity" />
        <activity android:name=".presentation.user_interface.screen.ReportActivity" />
        <activity android:name=".presentation.user_interface.screen.RecoveryActivity" />
        <activity android:name=".presentation.user_interface.screen.ProfileActivity" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>